// Feature modules live here, one folder per feature (see CLAUDE.md).
// Each exports a module with a static `DBOptions = { entities: [...] }`
// that app.module.ts spreads into the TypeORM entity list.
export {};
